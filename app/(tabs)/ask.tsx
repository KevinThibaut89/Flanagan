import { EmptyState, Screen } from '../../src/components/ui';

export default function AskScreen() {
  return (
    <Screen>
      <EmptyState title="Ask" message="Coming next." />
    </Screen>
  );
}
