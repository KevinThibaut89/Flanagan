import { EmptyState, Screen } from '../../src/components/ui';

export default function ScanScreen() {
  return (
    <Screen>
      <EmptyState title="Scan" message="Coming next." />
    </Screen>
  );
}
